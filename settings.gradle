pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'Chloz test'
include('app', 'repository', 'domain-model', 'services', 'common','dataaccess')
